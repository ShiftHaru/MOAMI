# BrowserDownloader 작업 지침

## 기획과 작업 기록의 기준 위치

개인 기록 경로는 Git에서 제외된 `.local/obsidian-path.txt`에서 읽는다. 이 파일에는 로컬 Obsidian 프로젝트 폴더 경로 한 줄만 저장한다. 파일이 없으면 사용자에게 기록 위치를 확인하며, 공개 파일에 개인 절대 경로를 기록하지 않는다.

이 경로의 철자 `BrowserDonwloader`를 유지한다. 코드 폴더 이름 `BrowserDownloader`와 다르다. 기획 원문과 작업 기록은 해당 Obsidian 폴더에서 관리하며 저장소에 복제하지 않는다.

## 작업 시작

1. Obsidian 폴더의 `시작하기.md`, `제품 요구사항.md`, `결정 기록.md`와 `작업 일지.md`의 최근 기록을 읽는다.
2. 관련 조사·구현·시험을 하기 전에 `기술 타당성.md`, `사용자 흐름.md`, `개발 및 검증 계획.md` 중 필요한 문서를 읽는다.
3. 기획의 확정 요구, 확인 사실, 설계 제안, 미검증 상태를 구분한다. 상세 요구의 기준은 Obsidian 노트다.

## 의미 있는 작업 후

- 조사·설계·구현·검증에서 실제 변화가 생기면 관련 노트와 `작업 일지.md`를 함께 갱신한다. 단순 조회만으로 완료 이력을 만들지 않는다.
- 각 노트의 `type`, `status`, `created`, `updated`를 유지한다. `created`는 보존하고 `updated`는 한국 시간의 실제 수정일로 갱신한다.
- REQ·DEC·TEST·SRC ID를 재사용하거나 재번호하지 않는다. 결정 변경 시 새 DEC와 대체 관계·이유를 남긴다.
- 확인한 출처·시험 환경·실제 결과·제한·다음 작업을 기록한다. 조사 완료를 구현 완료·실기기 검증 완료·배포 승인으로 확대하지 않는다.
- UTF-8(BOM 없음)으로 저장하고 한글·U+FFFD·BOM·필수 속성·내부 링크·ID 참조를 확인한다.
- 비밀키·쿠키·개인 콘텐츠와 인증 토큰을 포함한 URL을 노트나 공개 저장소에 남기지 않는다.
- Windows 사용자명, 개인 절대 경로, 개인 이메일을 추적 파일이나 커밋 작성자·커미터 정보에 포함하지 않는다. 공개 전 현재 파일뿐 아니라 공개 대상 Git 이력도 확인한다.
- 노트 경로에 접근할 수 없으면 기록을 갱신했다고 주장하지 않는다. 원인을 보고하고 관계없는 위치에 기획 사본을 만들지 않는다.

별도 Second Brain 프로그램 설치나 자동 검색 서비스 운영은 이 기록 체계에 포함하지 않는다. 이 파일은 기록 절차 안내이며 앱 구현·공개·배포를 자동으로 승인하는 문서가 아니다.

## Android 실기기 시험 종료

- 대상 앱 프로세스의 instrumentation 종료는 접근성 서비스를 시스템의 Crashed services에 남길 수 있다. 시험 PASS만으로 기기를 전달하지 않는다.
- 마지막 계측 후 최종 제품 APK에 `tools/verify_apk_update.py --adb <adb경로> --serial <기기> --apk <최종APK>`를 실행해 정상 재설치·Bound 연결·Crashed/Binding 부재·활성/동의 및 접근성 허용 유지까지 확인한다. 설정 토글이나 secure 설정 쓰기로 통과시키지 않는다.
- 이 최종 검사 이후 대상 instrumentation/접근성을 억제하는 uiautomator를 다시 실행하면 전달 검사를 다시 수행한다. 설치 후 APK 경로·해시·기기 연결 결과를 Obsidian에 기록한다.
